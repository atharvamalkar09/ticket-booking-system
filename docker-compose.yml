services:

  api:
    build:
      context: ./backend

    container_name: ticket_booking_api

    ports:
      - "8000:8000"

    env_file:
      - ./backend/.env

    volumes:
      - ./backend:/app